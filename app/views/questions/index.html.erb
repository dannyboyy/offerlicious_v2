<% @questions.each do |question| %>
  <div class="question row clearfix">
    <h2>
      <%= link_to question.title, question %><br>
      <small class="author">Submitted <%= time_ago_in_words(question.created_at) %> by <%= question.user.email %></small>
    </h2>
  </div>
<% end %>